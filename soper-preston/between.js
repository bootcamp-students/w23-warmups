function between(a, b) {
    //Set a variable to an empty array
    //Create a for loop to loop between the two numbers and print the result

    let numbers = [];

    for (let i = a; i <= b; i++) {
        numbers.push(i);
    }
    return numbers
}