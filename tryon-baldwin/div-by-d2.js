function isDivisible(n, x, y) {

  
    //  START
   //  READ n, x, y
    // IF (n is divisible by x AND n is divisible by y)
   //    SHOW true
  //   ELSE
   //    SHOW false
   //  ENDIF
    // END
  return n % x === 0 && n % y === 0
 }