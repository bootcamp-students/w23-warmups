function century(year) {
//   Finish this :)
//    return Math.floor(year/100) + ((year%100) ? 1 : 0)
//   return(century);
  
  return Math.ceil(year/100);
}


// need to century / year will output century