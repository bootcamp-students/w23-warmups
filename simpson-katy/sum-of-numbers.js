let a = 10;
let b = -5;
/*
receive two numbers
determine which number is the smaller of the two
put them in order
find all the numbers between the two numbers.
if numbers are equal just return one number
sum all the numbers between the two numbers
return only one number 
*/

function getSum(a, b) 
{
  if (a === b) {
    return a;
   } else if (a < b) {
    
   }
}