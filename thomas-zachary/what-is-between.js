function between(a, b) {
    // a always less than b
    // declare empty array
    //FOR a to b push num to array
    //return array
    
    let x = [];
    for (;a < b + 1; a++){
      x.push(a);
    }
    return x;
  }