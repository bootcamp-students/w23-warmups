//Function named between takes integers a and b
function between(a, b) {
  //Declare variable named arr with empty array
  let arr = [];
  //For loop iterating through arr
  for (let i = a; i <= b; i++) {
    //Push results of for loop to arr
    arr.push(i);
  }
  //Return variable arr
  return arr;
}
