SELECT CONCAT(prefix, ' ', first, ' ', last, ' ', suffix) as title
FROM names;


--Use a select statement
--Return a single column table...
  --Containing the full title of the person
  --concatenate all columns together except id
