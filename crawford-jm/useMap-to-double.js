function double(array) {
  //console.log(array);
    let newArr = array.map(num => num * 2);
    //console.log(newArr);
  return newArr;
}


//Function takes an array of integers
//Return an array where each integer is doubled in value
