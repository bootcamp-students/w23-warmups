//Class makes object kata
class Kata {
    //Statis declares method l, w, h
    static getVolumeOfCuboid(length, width, height) {
      //Return calculation of l * w * h
      return length * width * height
    }
  }
